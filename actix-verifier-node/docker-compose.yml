version: '3.8'
services:
  zk-verifier-node:
    build: .
    container_name: mesh-zk-verifier
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - MESH_PORT=8080
      - ZK_STRICT_MODE=true
