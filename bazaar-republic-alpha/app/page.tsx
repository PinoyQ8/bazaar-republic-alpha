// Location: app/page.tsx
import { redirect } from "next/navigation";

export default function RootSector() {
  // 🛡️ ZERO-CLICK E-NETWORK TUNNEL
  // Instantly routes Real Pioneers from the passive root to the active Dashboard AuthGate.
  redirect("/dashboard");
}