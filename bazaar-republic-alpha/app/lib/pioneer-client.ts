export async function pioneerFetch(sector: string, options: RequestInit = {}) { const baseUrl = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000'; const url = ${baseUrl}/api/proxy; const response = await fetch(url, { ...options, method: options.method || 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': process.env.PI_API_KEY || '', 'x-target-sector': sector, ...options.headers } }); if (!response.ok) { throw new Error(MESH_FRACTURE: Sector  access denied.); } return response.json(); }
